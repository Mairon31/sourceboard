name: Task 1 edit round-trip GREEN

on:
  push:
    branches:
      - feature/community-cosmetics-profile-identity-emotes

permissions:
  contents: write

jobs:
  edit-round-trip-green:
    if: "${{ github.event.head_commit.message != 'feat(comments): round-trip emotes when editing' }}"
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v7
        with:
          ref: feature/community-cosmetics-profile-identity-emotes
      - uses: actions/setup-node@v7
        with:
          node-version: 24
          cache: npm
      - run: npm ci
      - name: Apply edit round-trip implementation
        run: python .github/scripts/apply-task1-edit-roundtrip.py
      - name: Format changed files
        run: npx prettier --write shared/richtext/markdown.ts app/components/product/CommentThread.tsx tests/unit/markdown-richtext.test.ts tests/unit/media-picker.test.ts
      - name: Run focused comment/edit tests
        run: npm test -- tests/unit/comment-richtext.test.ts tests/unit/markdown-richtext.test.ts tests/unit/media-picker.test.ts
      - name: Typecheck
        run: npm run typecheck
      - name: Verify diff
        run: git diff --check
      - name: Commit verified implementation
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add shared/richtext/markdown.ts app/components/product/CommentThread.tsx tests/unit/markdown-richtext.test.ts tests/unit/media-picker.test.ts
          if git diff --cached --quiet; then
            exit 0
          fi
          git commit -m "feat(comments): round-trip emotes when editing"
          git push origin HEAD:feature/community-cosmetics-profile-identity-emotes
