name: Format Block B Task 2 RED

on:
  push:
    branches:
      - feature/platform-overhaul-block-b

permissions:
  contents: write

jobs:
  format-red:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v7
        with:
          ref: feature/platform-overhaul-block-b
      - uses: actions/setup-node@v7
        with:
          node-version: 24
          cache: npm
      - run: npm ci --strict-allow-scripts
      - name: Format Task 2 RED and remove helper workflow
        run: |
          npx prettier --write tests/unit/public-profile-access.test.ts
          rm .github/workflows/format-block-b-task2-red.yml
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add tests/unit/public-profile-access.test.ts .github/workflows/format-block-b-task2-red.yml
          git commit -m "style: format Block B Task 2 RED"
          git push origin HEAD:feature/platform-overhaul-block-b
